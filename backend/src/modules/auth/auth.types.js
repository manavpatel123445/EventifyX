/**
 * @typedef {Object} AuthResponse
 * @property {Object} user - Sanitized user info
 * @property {string} user._id - User unique ID
 * @property {string} user.name - User display name
 * @property {string} user.email - User email address
 * @property {string} user.role - User role
 * @property {string} accessToken - Short-lived JSON Web Token
 * @property {string} refreshToken - Long-lived Refresh JSON Web Token
 */

export {};
